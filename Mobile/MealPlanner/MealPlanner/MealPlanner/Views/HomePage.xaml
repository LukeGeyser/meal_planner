<?xml version="1.0" encoding="utf-8" ?>
<MasterDetailPage
    x:Class="MealPlanner.Views.HomePage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:MealPlanner.Views"
    BackgroundColor="{DynamicResource BackGroundColor}">

    <MasterDetailPage.Master>
        <views:MenuPage x:Name="masterPage" />
    </MasterDetailPage.Master>

    <MasterDetailPage.Detail>
        <NavigationPage BarBackgroundColor="{DynamicResource BtnHeaderText}" IconImageSource="icon.png">
            <NavigationPage.Icon>
                <OnPlatform x:TypeArguments="FileImageSource">
                    <On Platform="iOS" Value="tab_feed.png" />
                </OnPlatform>
            </NavigationPage.Icon>
            <x:Arguments>
                <views:MainPage IconImageSource="icon.png" />
            </x:Arguments>
        </NavigationPage>
    </MasterDetailPage.Detail>
</MasterDetailPage>